node_modules
.mastra
.next
dist
data
.env
.env.*
!.env.example
.git
.claude
*.log

# frontend/ ist NICHT ausgeschlossen: der Frontend-Build laeuft aus diesem
# Kontext (frontend/Dockerfile), und der Agent-Build braucht
# frontend/package.json fuer `npm ci` im Workspace.
frontend/node_modules
frontend/.next
